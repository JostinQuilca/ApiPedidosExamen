DATABASE_URL=postgres://username:password@host:5432/database_name
CORS_ORIGIN=https://mi-frontend.example.com
# Example .env for local development / Render
# Set your NeonDB / Postgres connection string here (do NOT commit real creds)
DATABASE_URL=postgresql://USER:PASS@HOST:PORT/DB?sslmode=require
PORT=3000
# Optional: restrict CORS in production (e.g. https://yourdomain.com)
CORS_ORIGIN=
